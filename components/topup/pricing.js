import React from 'react';
import { 
    View,
    Text
} from 'react-native';

const Pricing = ({ containerStyle, title, titleStyle, leftComponent, rightComponent }) => {
    return (
     <></>
    )
}

export default Pricing